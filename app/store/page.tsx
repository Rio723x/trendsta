import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Store',
    description: 'Browse the Trendsta store for tools, templates, and resources to accelerate your content growth journey.',
};

export default function Store() {
    return (
        <div>
            <h1>Store</h1>
        </div>
    );
}